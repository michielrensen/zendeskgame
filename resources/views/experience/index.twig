{% extends "default.twig" %}

{% block content %}
  <div class="row">
    {% for item in list %}
    <div class="col-xs-6 col-md-3">
      <a href="#" class="thumbnail">
        {{ item.name }}: {{ item.experience }}
      </a>
    </div>
    {% endfor %}
  </div>
{% endblock %}